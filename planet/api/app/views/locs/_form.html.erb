<%= form_with(model: loc, local: true) do |form| %>
  <% if loc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(loc.errors.count, "error") %> prohibited this loc from being saved:</h2>

      <ul>
      <% loc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :x %>
    <%= form.number_field :x %>
  </div>

  <div class="field">
    <%= form.label :y %>
    <%= form.number_field :y %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
